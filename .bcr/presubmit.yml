bcr_test_module:
  module_path: "tests"
  matrix:
    bazel: ["8.x", "9.x"]
    platform: ["debian11", "ubuntu2204", "windows", "macos", "macos_arm64"]
  tasks:
    smoke_tests:
      name: "Run smoke tests"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
